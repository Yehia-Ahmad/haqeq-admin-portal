import { DateToArabicPipePipe } from './date-to-arabic-pipe.pipe';

describe('DateToArabicPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateToArabicPipePipe();
    expect(pipe).toBeTruthy();
  });
});
